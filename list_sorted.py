courses = ['Art', 'Polity', 'Chemistry', 'Biology', 'Socials']

sorted(courses)
print(courses)  # just sort the list

sorted_courses = sorted(courses)

print(courses)  # original list

print(sorted_courses)  # sorts the list in ascending order w/o altering the original list
                        # the sorted list will be stored in new variable