# here we are going to see the python library functions.

import math  # importing math library into this file.

def square_root_of_num(num):
    return int(math.sqrt(num))       # from math library we are using sqrt() function.

print(square_root_of_num(4))