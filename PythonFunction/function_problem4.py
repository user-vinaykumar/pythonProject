# write a program to take a list as an input and remove the duplicates and output
# should be in set.

def duplicateEliminator(item):
    noDuplicates = set(item)
    print(noDuplicates)


duplicateEliminator([1, 2, 3, 3, 4, 5])
